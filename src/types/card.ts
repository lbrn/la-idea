export default interface cardWrapper {
	grantLink: string;
	id: string;
	img: string;
	imgTitle: string;
	institution: string;
	institutionLink: string;
	investigator: string;
	investigatorLink: string;
	title: string;
}